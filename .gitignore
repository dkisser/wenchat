node_modules/
dist/
# @yao-pkg/packer output (single-binary releases)
dist-release/
# Node SEA blob artifacts (in case we ever drop to raw SEA)
*.blob
# Top-level accidental packer output (safety net; packer normally writes to dist-release/)
/wenchat
/wenchat.exe
# Legacy binary lockfile from pre-1.2 bun. The text lockfile (bun.lock) IS
# tracked: CI installs with --frozen-lockfile, so keep it in sync with package.json.
bun.lockb
# Internal design docs / plans / specs. Lives in-repo for the maintainer's
# reference but shouldn't be in the published history.
docs/superpowers/
*.log
.DS_Store
.env
#claude
.claude/
# Local agent state: imported skills + local AGENTS.md copy. Machine-local; keep out of git.
.agents/
